@TTL |Factorial|
@AREA |Program| @CODE, @READWRITE
@ENTRY

    Start
            MOV     R0, PC
            LDRH    R0, [R0, `Tbl]
            EOR     R1, R1
            LDRH    R1, [R1, `Value]
            LSL     R1, R1, #0x1
            ADD     R0, R0, R1
            MOV     R2, R0
            MOV     R3, PC
            STR     R1, [R3, `Result]
            SWI     &11

@END

@AREA |Data| @DATA, @READWRITE
@ENTRY

    Tbl
            @DCHW   #1
            @DCHW   #1
            @DCHW   #2
            @DCHW   #6
            @DCHW   #24
            @DCHW   #120
            @DCHW   #720
            @DCHW   #5040
    Value   @DCHW   #5
    Result  @DCHW   #0

@END